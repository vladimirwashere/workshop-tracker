<!DOCTYPE html>
<html lang="<%= I18n.locale %>">
<head>
  <title><%= content_for(:title) || t("common.app_name") %></title>
  <meta name="viewport" content="width=device-width,initial-scale=1">
  <meta charset="utf-8">
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>
  <link rel="icon" href="/icon.png" type="image/png" sizes="any">
  <link rel="apple-touch-icon" href="/icon.png">
  <%= stylesheet_link_tag :app %>
  <%= javascript_importmap_tags %>
</head>
<body class="bg-gradient-to-br from-slate-900 via-blue-900 to-indigo-900 min-h-screen flex items-center justify-center">
  <div class="w-full max-w-md px-6">
    <div class="text-center mb-8">
      <div class="w-24 mx-auto mb-3 rounded-2xl bg-white/10 backdrop-blur-sm p-3 border border-white/20 shadow-lg">
        <%= image_tag "logo.png", alt: "Company Logo", class: "w-full h-auto" %>
      </div>
      <h1 class="text-2xl font-bold text-white tracking-tight"><%= t("common.app_name") %></h1>
    </div>

    <% if flash[:notice] %>
      <div class="flash-notice mb-4" role="alert">
        <p><%= flash[:notice] %></p>
      </div>
    <% end %>
    <% if flash[:alert] %>
      <div class="flash-alert mb-4" role="alert">
        <p><%= flash[:alert] %></p>
      </div>
    <% end %>

    <div class="glass-card p-8">
      <%= yield %>
    </div>
  </div>
</body>
</html>
