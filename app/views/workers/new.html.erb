<% content_for(:title) { t("workers.new_title") } %>

<%= render "shared/back_link", fallback: workers_path %>

<div class="max-w-2xl mx-auto">
  <h1 class="page-title mb-6"><%= t("workers.new_title") %></h1>

  <div class="glass-card p-6">
    <%= render "form", worker: @worker %>
  </div>
</div>
