<% content_for(:title) { t("material_entries.edit_title") } %>

<%= render "shared/back_link", fallback: material_entries_path %>

<div class="max-w-3xl mx-auto">
  <div class="mb-6">
    <h1 class="page-title"><%= t("material_entries.edit_title") %></h1>
    <p class="mt-1 text-sm text-slate-500"><%= @project.name %></p>
  </div>

  <div class="glass-card p-6">
    <%= render "form", material_entry: @material_entry %>
  </div>

  <%= render "attachments/attachments_section", related: @material_entry %>
</div>
